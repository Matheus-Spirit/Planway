server.port=${PORT:8080}

spring.application.name=trabalhoInterdiciplinar
spring.datasource.url=${DB_URL:jdbc:mysql://${DB_HOST:mysql.railway.internal}:${DB_PORT:3306}/${DB_NAME:railway}}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:sksvgwnaopDqxZPNKDGpnYKopscGZNpv}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# CORS
cors.allowed-origins=https://planway-one.vercel.app,http://localhost:8080

# Configurações do JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf
spring.thymeleaf.check-template-location=false

# Configuração do JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000
